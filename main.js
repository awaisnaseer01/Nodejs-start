let user = {
    name : "Ben Stokes",
    email: "ben51@england.com",
    age : 54,
    team: "Eng"

};

const createPlayer = (player) => {
    console.log(player);
};

let arr = ["haris rauf", "shaheen afridi", "sam curran"]

const createBowlers =(bowlers) => {
    console.log(bowlers);
};

createPlayer(user);
createBowlers(arr);